fix: use Google OAuth (GBP API) for coordinates instead of Places API

- Add getValidAuthForTenant(tenantId) helper to get valid OAuth for a specific tenant
- Add fetchGbpLocationsWithCoordinates() to fetch lat/lng from Google Business
  Profile API (uses existing OAuth connection, no extra API key needed)
- Update sync-listings to:
  1. First try GBP API coordinates (via placeId match)
  2. Fall back to Google Places API if GBP fails
- GBP coordinates are stored in DB and returned via /api/embedsocial/locations
- Users no longer need a separate Google Places API key
