fix: SEO optimization API key priority and settings fallback

- SEO optimization endpoint: prioritize Railway env vars over DB
  (process.env.GEMINI_API_KEY || tenant.geminiApiKey) since DB is wiped
  on every Railway redeploy while env vars persist
- /api/settings GET: add safe fallback (findFirst()) when req.tenantId
  is undefined, and merge env vars into response for resilience
- /api/settings POST: use safe tenant lookup when req.tenantId missing
