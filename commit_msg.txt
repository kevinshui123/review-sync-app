fix: all EmbedSocial endpoints use env vars priority + safe tenantId fallback

Fixes GBP Performance Data / chart-data showing empty on Reports page.

- /api/embedsocial/chart-data: env vars priority, safe tenantId fallback
- /api/embedsocial/metrics: env vars priority, safe tenantId fallback
- /api/reports/gbp-pdf: env vars priority
- getReportChartData: tenantId can be null/undefined
- All three endpoints now read process.env.EMBEDSOCIAL_API_KEY first
  (survives Railway DB wipe on redeploy), then fall back to DB
