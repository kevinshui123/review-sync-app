# XHS API Service
# Flask application with Spider_XHS integration

from app import app

if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5000))
    print(f"🚀 Starting XHS API Service on port {port}")
    app.run(host='0.0.0.0', port=port, debug=False)
